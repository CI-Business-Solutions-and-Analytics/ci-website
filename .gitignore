# OS / editor
.DS_Store
Thumbs.db
*.log

# Dependencies (none currently, but safe to ignore)
node_modules/

# Local preview helpers / local-only settings (not needed in production)
.claude/server.js
.claude/settings.local.json

# Working draft copy of the site (not for deploy)
draft/
